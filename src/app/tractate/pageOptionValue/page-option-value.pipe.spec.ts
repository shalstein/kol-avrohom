import { PageOptionValuePipe } from './page-option-value.pipe';

describe('PageOptionValuePipe', () => {
  it('create an instance', () => {
    const pipe = new PageOptionValuePipe();
    expect(pipe).toBeTruthy();
  });
});
